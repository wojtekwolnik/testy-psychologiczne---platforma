# Test Environment Variables
# Requires Docker Desktop running: docker compose up -d db_test

DATABASE_URL="postgresql://postgres:password@localhost:5433/testy_psychologiczne_test"
JWT_SECRET="test-secret"
